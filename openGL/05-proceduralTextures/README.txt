To run this code, you need to install the DevIL image manipulation
libraries from 

  https://github.com/DentonW/DevIL

Be sure to install the *development* version of the libraries.

On Linux, you can simply execute this:

  sudo apt-get install libdevil-dev
